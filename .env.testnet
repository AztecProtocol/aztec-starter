ENV=testnet

# Node Configuration
# For sandbox (default): Leave unset or use http://localhost:8080
# For testnet: Set to https://aztec-alpha-testnet-fullnode.zkv.xyz
NODE_URL=https://aztec-alpha-testnet-fullnode.zkv.xyz

# L1 Configuration (required for testnet)
L1_RPC_URL=https://ethereum-sepolia-rpc.publicnode.com
L1_CHAIN_ID=11155111  # Sepolia

# Contract Addresses (set after deployment)
VOTING_CONTRACT_ADDRESS=

# Account Configuration (set after creating account)
SECRET=
SALT=

# Optional: Skip sandbox startup in tests
# SKIP_SANDBOX=true