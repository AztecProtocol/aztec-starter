[package]
name = "leader_aggregator"
type = "bin"
version = "0.1.0"
authors = ["aztec-starter"]
compiler_version = ">=0.18.0"

[dependencies]
# Use the compiler-bundled standard library (no external dependency).
